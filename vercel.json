{
  "version": 2,
  "buildCommand": "cd mais-vaquejada && npm install && npm run build",
  "outputDirectory": "mais-vaquejada/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
