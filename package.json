{
  "dependencies": {
    "@capacitor/app": "^8.1.0",
    "@capacitor/browser": "^8.0.3",
    "@capacitor/core": "^8.3.1"
  },
  "scripts": {
    "build": "cd mais-vaquejada && npm install && npm run build"
  }
}
